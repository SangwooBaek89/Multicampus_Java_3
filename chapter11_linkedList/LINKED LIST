링크드가 필요한 이유

여러 개의 데이터를 다룸 ---> 배열
그러나 배열은 장/단점이 있음
장 : index로 접근 쉬움
단 : 길이가 고정 / 삽입, 삭제시 배열 전체 복사해야 함

링크드 리스트
장점 : 데이터 개수에 영향을 받지 않음
단점 : 인덱스로 바로 접근하는 것이 아니라 처음부터 찾아감

끝에 이는 노드 삽입은 시간 복잡도 O(1)로 바꿀 것


더블 링크드 리스트
1. prev 노드 필요

환형 링크드 리스트
1. 마지막 노드 넥스트는 헤드
2. 처음 노드 이전은 마지막 노드

환형 더블 링크드 리스트